app:
  name: app-pipe
  ide: none

  config:
    minimum_wio_version: 0.3.2
    supported_platforms:
    - all
    supported_frameworks:
    - all
    supported_boards:
    - all

  compile_options:
    platform: all

targets:
  default: main
  create:
    main:
      src: src
      platform: native
      framework: all
      board: all

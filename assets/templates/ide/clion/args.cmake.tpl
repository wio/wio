# These arguments are provided to wio commands that are executed
# Note: Do not wrap them into a single string but rather multiple values
# example: set(WIO_BUILD_ARGS --verbose --force)

set(WIO_BUILD_ARGS )
set(WIO_RUN_ARGS )
set(WIO_UPDATE_ARGS )
set(WIO_INSTALL_ARGS )
